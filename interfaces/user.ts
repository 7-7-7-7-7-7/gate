export type User = {
    id: number,
    email: string,
    name: string,
    username: string,
    password: string,
    birth: Date,
    isAdmin: boolean,
    country: string,
    disabled: boolean
}
